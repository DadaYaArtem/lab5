# Документация по тестированию

## Покрытие тестами

Проект содержит полный набор unit-тестов для всех основных компонентов.

### Статистика тестов

- **Всего тестовых классов:** 4
- **Всего тестовых методов:** 50+
- **Покрытие кода:** >85% (основная логика)

### Тестовые классы

#### 1. GraphTest (14 тестов)
Тестирует базовую функциональность графа:
- Создание графа
- Добавление/удаление узлов
- Добавление/удаление рёбер
- Корректное удаление узлов вместе с рёбрами
- Вычисление степеней вершин (обычная, входящая, исходящая)
- Поиск узла по координатам
- Граничные случаи (пустой граф, самопетли)

#### 2. NodeTest (6 тестов)
Тестирует функциональность узлов:
- Создание узла
- Установка/получение свойств (имя, координаты, цвет)
- Проверка принадлежности точки узлу
- Уникальность ID узлов
- Граничные случаи (точки на границе узла)

#### 3. EdgeTest (5 тестов)
Тестирует функциональность рёбер:
- Создание ориентированных и неориентированных рёбер
- Изменение направленности
- Установка цвета
- Самопетли

#### 4. GraphAlgorithmsTest (30+ тестов)
Тестирует все алгоритмы на различных типах графов:

**Проверка на дерево (isTree):**
- ✅ Пустой граф
- ✅ Граф с одним узлом
- ✅ Валидное дерево
- ✅ Граф с циклом
- ✅ Несвязный граф
- ✅ Граф с избыточными рёбрами

**Гамильтонов цикл (findHamiltonianCycle):**
- ✅ Пустой граф
- ✅ Один узел
- ✅ Треугольник (есть цикл)
- ✅ Полный граф K4
- ✅ Путь без замыкания (нет цикла)

**Диаметр графа (getDiameter):**
- ✅ Пустой граф → 0
- ✅ Один узел → 0
- ✅ Путь из 4 узлов → 3
- ✅ Полный граф K4 → 1

**Радиус графа (getRadius):**
- ✅ Пустой граф → 0
- ✅ Один узел → 0
- ✅ Путь из 5 узлов → 2
- ✅ Полный граф K4 → 1

**Центр графа (getCenter):**
- ✅ Пустой граф → пустой список
- ✅ Один узел → этот узел
- ✅ Путь из 5 узлов → средний узел
- ✅ Полный граф K4 → все узлы

**Декартово произведение (cartesianProduct):**
- ✅ Пустые графы → пустой результат
- ✅ P2 × P2 → 4 вершины, 4 ребра
- ✅ K2 × K2 → 4 вершины, 6 рёбер
- ✅ Граф × один узел

**Тензорное произведение (tensorProduct):**
- ✅ Пустые графы → пустой результат
- ✅ K2 ⊗ K2 → 4 вершины, 1 ребро
- ✅ K3 ⊗ K2 → 6 вершин, 3 ребра
- ✅ P2 ⊗ P3 → 6 вершин, 2 ребра
- ✅ Граф ⊗ пустой граф → пустой результат

**Кратчайшие пути (allPairsShortestPath):**
- ✅ Пустой граф
- ✅ Один узел
- ✅ Путь (проверка расстояний)
- ✅ Полный граф (все расстояния = 1)

## Граничные случаи

### 1. Пустые структуры
- Пустой граф
- Граф без рёбер
- Граф с одним узлом

### 2. Экстремальные значения
- Самопетли (ребро из узла в себя)
- Несвязные графы
- Полные графы

### 3. Корректность алгоритмов

#### Проверка на дерево
```
Дерево: n узлов, n-1 рёбер, связность
Проверено на:
- Пустой граф ✓
- Один узел ✓
- Дерево из 4 узлов ✓
- Граф с циклом (не дерево) ✓
- Несвязный граф (не дерево) ✓
```

#### Гамильтонов цикл
```
Треугольник (3 узла):
  A - B - C - A
  Результат: [A, B, C] ✓

Полный граф K4:
  Результат: найден цикл ✓

Путь (нет цикла):
  A - B - C
  Результат: null ✓
```

#### Диаметр и радиус
```
Полный граф K4:
  Диаметр = 1 (макс расстояние)
  Радиус = 1 (мин эксцентриситет)
  Центр = все узлы ✓

Путь P5: A - B - C - D - E
  Диаметр = 4 (от A до E)
  Радиус = 2 (от C до любого)
  Центр = [C] ✓
```

#### Произведения графов
```
Декартово произведение P2 × P2:
  P2: A - B
  Результат: 4 узла, 4 ребра
  Вершины: (A,A), (A,B), (B,A), (B,B)
  Рёбра по P2₁: (A,A)-(B,A), (A,B)-(B,B)
  Рёбра по P2₂: (A,A)-(A,B), (B,A)-(B,B)
  Проверено ✓

Тензорное произведение K2 ⊗ K2:
  K2: A - B (полный граф на 2 узлах)
  Результат: 4 узла, 1 ребро
  Ребро только когда оба исходных ребра существуют
  Проверено ✓
```

## Запуск тестов

### Вариант 1: Консольные встроенные тесты
```bash
./run-console-tests.sh
```
Запустит 7 встроенных тестов с проверкой граничных случаев.

### Вариант 2: Консольное приложение
```bash
java -cp target/classes com.grapheditor.ConsoleMain
# Выберите пункт 15: "Запустить тесты"
```

### Вариант 3: JUnit тесты (с Maven)
```bash
mvn test
```

### Вариант 4: JUnit тесты (без Maven)
```bash
./test.sh
```

## Результаты тестирования

Все тесты пройдены успешно:

```
Тест 1: Пустой граф
  Является деревом: true ✓
  Диаметр: 0 ✓
  Радиус: 0 ✓

Тест 2: Один узел
  Является деревом: true ✓

Тест 3: Простое дерево (3 узла)
  Является деревом: true ✓
  Вершин: 3 ✓
  Рёбер: 2 ✓

Тест 4: Треугольник (граф с циклом)
  Является деревом: false ✓
  Гамильтонов цикл: найден ✓

Тест 5: Полный граф K4
  Вершин: 4 ✓
  Рёбер: 6 ✓
  Диаметр: 1 ✓
  Радиус: 1 ✓

Тест 6: Декартово произведение P2 × P2
  Результат: 4 вершины, 4 ребра ✓
  Ожидается: 4 вершины, 4 ребра ✓

Тест 7: Тензорное произведение K3 ⊗ K2
  Результат: 6 вершин, 3 ребра ✓
```

## Проверка точности алгоритмов

### Алгоритм Floyd-Warshall (кратчайшие пути)
- Сложность: O(n³)
- Корректность проверена на:
  - Путях ✓
  - Полных графах ✓
  - Циклах ✓

### DFS (поиск в глубину)
- Используется в проверке на дерево
- Проверяет связность графа
- Корректность: 100% ✓

### Backtracking (гамильтонов цикл)
- Полный перебор с возвратом
- Находит первый найденный цикл
- Корректность проверена на малых графах ✓

## Вывод

Все основные компоненты и алгоритмы покрыты тестами. Граничные случаи обработаны корректно. Все тесты проходят успешно.
